1. 모델 선정 -> KoELECTRA
- 긴 문장 넣고 감정 단어 추출 테스트

2. 감정 단어 추출 기능
- 입력받은 문장 KoELECTRA 토크나이저로 분리 (토큰화) -> 감정 단어 리스트 반환
- 감정 단어 리스트에서 감정 카테고리 추출

3. 추가 기능
- 그래프 시각화: 감정 단어 빈도/추이 그래프 생성 (matplotlib 계열?)
- 공감 메시지 표시:
  - 감정 단어 -> 감정 카테고리 매핑
  - LLM 활용해 감정 카테고리 별 공감 메시지 리스트 작성

4. 기술
- 프론트: 문장 입력 -> 결과 화면과 공감메시지 -> 시각화 보여주는 화면
- 백: 모델 호출 및 결과 반환 (파이썬)
- DB: 날짜 별 감정 단어 기록 저장, 작성한 일기도 보여줌?
- 시각화 모듈: DB 데이터 기반 그래프 생성 (파이썬)
- 모델이랑 시각화 모듈 생각하면 어지간한 부분은 파이썬으로 처리하면 될 것 같은데 DB랑 프론트는 뭘로 해야 할 지 모르겠음
    - 사실 날짜-감정단어 목록만 간단하게 쓸 거니까 데이터베이스는 SQL까지 안 가고 그냥 파이썬이나 json... csv... 선에서 해결해도 될 것 같긴 함
    - 프론트엔드는? 잘 모르겠음... 여기가 중요한 파트가 아니라서 최대한 간단했으면 좋겠는데


---
아래는 위 계획 기반으로 GPT한테 뽑아달라고 한 할 일 목록

## ✅ 할 일 목록

### 1. 모델 준비 및 테스트
- [ ] KoELECTRA 모델 선정 및 불러오기 (`transformers` 활용)
- [ ] 긴 문장 입력 → 감정 단어 추출 테스트
- [ ] 토크나이저 동작 확인 (토큰화 결과 확인)

### 2. 감정 단어 추출 기능 구현
- [ ] 입력 문장 토큰화 → 감정 단어 리스트 반환
- [ ] 감정 단어 리스트에서 감정 카테고리 추출 (예: 슬픔, 기쁨, 분노 등)
- [ ] 감정 단어 → 감정 카테고리 매핑 로직 작성

### 3. 추가 기능 개발
- [ ] **그래프 시각화**: 감정 단어 빈도/추이 그래프 생성 (`matplotlib` 또는 `plotly`)
- [ ] **공감 메시지 표시**:
  - 감정 카테고리별 메시지 템플릿 작성
  - 필요 시 LLM 활용해 자동 메시지 생성

### 4. 서비스 구조 설계
- **프론트엔드**
  - [ ] 문장 입력 화면
  - [ ] 결과 화면 (감정 단어 + 공감 메시지 + 그래프)
  - 👉 간단히 하려면 `Streamlit` 추천 (빠르게 웹 UI 구현 가능)
- **백엔드**
  - [ ] Python 기반 서버 (Flask 또는 FastAPI)
  - [ ] 모델 호출 및 결과 반환 API 작성
- **데이터 저장**
  - [ ] 날짜별 감정 단어 기록 저장
  - [ ] 작성한 일기와 감정 기록 조회 기능
  - 👉 단순히 하려면 `JSON` 또는 `CSV` 파일 저장으로 충분 (SQL DB는 선택사항)
- **시각화 모듈**
  - [ ] DB/파일 데이터 기반 그래프 생성
  - [ ] 프론트엔드에 그래프 표시
